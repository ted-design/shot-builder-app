# Example environment variables for Firebase configuration.
#
# Use this file as a reference for required keys. In CI/CD, set these as
# GitHub Secrets so they are available during `npm run build`.
# For local development, you may copy this file to `.env` (or
# `.env.development.local`) and populate with values from Firebase Console →
# Project Settings → General → Your Web App's SDK configuration.
#
# Never commit your real credentials to version control.

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=
